general:
  branches:
    ignore:
      - master

machine:
  services:
    - docker

dependencies:
  override:
    - make install-test-deps

test:
  override:
    - make check-version
    - make test-ci
