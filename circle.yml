general:
  branches:
    ignore:
      - master

machine:
  services:
    - docker

dependencies:
  override:
    - echo "No dependencies."

test:
  override:
    - make check-version
    - make test-ci
